pre-commit:
  commands:
    check:
      root: "auth-proxy/"
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css}"
      run: bunx @biomejs/biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --unsafe {staged_files}
      stage_fixed: true